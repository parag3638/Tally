# Dependencies are now declared in pyproject.toml (single source of truth).
#
#   pip install .          # runtime dependencies
#   pip install .[dev]     # + test / lint tooling
#
# This file is kept only as a pointer for tools that look for it.
-e .
